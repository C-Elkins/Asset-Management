# API Configuration
VITE_API_BASE=/api

# Google OAuth 2.0 Configuration
# To enable Google Sign-In, get your client ID from:
# https://console.cloud.google.com/apis/credentials
# 1. Create a new OAuth 2.0 Client ID
# 2. Set authorized JavaScript origins: http://localhost:3005, https://yourdomain.com
# 3. Set authorized redirect URIs: http://localhost:3005/auth/google/callback, https://yourdomain.com/auth/google/callback
VITE_GOOGLE_CLIENT_ID=your-google-client-id-here

# Microsoft OAuth / Azure AD Configuration
# To enable Microsoft Sign-In, get your client ID from:
# https://portal.azure.com/#blade/Microsoft_AAD_RegisteredApps/ApplicationsListBlade
# 1. Register a new application in Azure AD
# 2. Set redirect URI: http://localhost:3005/auth/microsoft/callback, https://yourdomain.com/auth/microsoft/callback
# 3. Enable ID tokens under Authentication
# 4. Add API permissions: User.Read, openid, profile, email
# Tenant ID options:
# - 'common' - Multi-tenant (work/school + personal Microsoft accounts)
# - 'organizations' - Multi-tenant (work/school accounts only)
# - 'consumers' - Personal Microsoft accounts only
# - '{tenant-id}' - Specific Azure AD tenant
VITE_MICROSOFT_CLIENT_ID=your-microsoft-client-id-here
VITE_MICROSOFT_TENANT_ID=common

# Sentry (optional)
VITE_SENTRY_DSN=
VITE_SENTRY_TRACES_SAMPLE_RATE=1.0
VITE_SENTRY_ENV=dev
