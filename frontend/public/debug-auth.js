// Debug utility to clear all auth-related storage
// Run this in the browser console: clearAuthDebug()\n\nwindow.clearAuthDebug = function() {\n  console.log('🧹 Clearing all auth-related storage...');\n  \n  // Clear localStorage\n  localStorage.removeItem('auth-store');\n  localStorage.removeItem('jwt_token');\n  \n  // Clear sessionStorage\n  sessionStorage.removeItem('E2E');\n  sessionStorage.removeItem('corr_id');\n  \n  // Clear all localStorage items that might be related\n  Object.keys(localStorage).forEach(key => {\n    if (key.includes('auth') || key.includes('token') || key.includes('user')) {\n      localStorage.removeItem(key);\n      console.log('Removed:', key);\n    }\n  });\n  \n  console.log('✅ Auth storage cleared. Reloading page...');\n  setTimeout(() => window.location.reload(), 500);\n};\n\nwindow.debugAuth = function() {\n  console.log('🔍 Current auth state:');\n  console.log('localStorage:', {\n    'auth-store': localStorage.getItem('auth-store'),\n    'jwt_token': localStorage.getItem('jwt_token') ? '[TOKEN_PRESENT]' : null\n  });\n  console.log('sessionStorage corr_id:', sessionStorage.getItem('corr_id'));\n};\n\nconsole.log('🛠️ Auth debug utilities loaded. Available commands:');\nconsole.log('- clearAuthDebug() - Clear all auth storage');\nconsole.log('- debugAuth() - Show current auth state');\n